const mocha = require('mocha') ;
const assert = require('assert')

describe('testez sa vad daca merge', function(){
    it('adds two numbers together', function(){
        assert(2 + 3 === 5) ;
    });
})